[phases.setup]
nixPkgs = ["python311", "nodejs-18_x"]

[phases.install]
cmds = ["pip install -r requirements-api.txt", "npm install"]

[phases.build]
cmds = []

[start]
cmd = "sh -c 'PYTHONPATH=. uvicorn src.generalized.dev_server:app --host 0.0.0.0 --port ${PORT:-8001}'"
