[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements-api.txt"

[deploy]
startCommand = "uvicorn src.api_server:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "on_failure"
